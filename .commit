perf(gold-fidelity): cache reusable reocr evidence

- add conservative snapshot-local ReOCR evidence cache
- cache only decisive verified_expected / verified_observed outcomes
- clone cache hits with explicit evidence_cache_hit provenance
- expose cache hit/miss/write/saved counters in validator detail
- add focused smoke tests for cache keying and cached evidence provenance
