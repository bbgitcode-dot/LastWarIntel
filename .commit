Sentinel v0.9.5.96

Sprint: 551 Gold Fidelity Gate

Changes:
- Added Gold Fidelity blocker metrics to ground_truth_validator.py.
- Added gold_fidelity_ready summary flag.
- Added gold_fidelity_blockers JSON section and Excel sheet.
- Refined character verification so stable exact confusable characters are not default blockers.
- Preserved case-sensitive alliance tag verification.
- Updated /docs for 551 Gold Fidelity mode.

Validation:
- 5 passed – character verification + validator smoke tests.
- 551 GT validator OK against current lastwar_export.xlsx.
- py_compile OK for touched files.
- zip integrity OK.

Commit command:
git add .
git commit -m "feat(data-guard): add 551 gold fidelity blockers"

Tag command:
git tag -a v0.9.5.96 -m "v0.9.5.96 551 Gold Fidelity Gate"
